# Build-time configuration for the admin panel.
# Copy to .env. No default exists for the API base — a guessed one would
# point this church's admin panel at another church's data.
PUBLIC_API_BASE=https://api.example.org
